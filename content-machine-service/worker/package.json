{
  "name": "content-machine-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env worker.mjs",
    "test": "node render.test.mjs && node reddit.test.mjs && node directories.test.mjs",
    "inspect": "node --env-file-if-exists=.env directories.mjs inspect"
  },
  "dependencies": {
    "playwright": "1.56.1",
    "sharp": "0.35.4"
  },
  "engines": {
    "node": ">=22"
  }
}
