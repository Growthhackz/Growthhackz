import {ArrowLeft,Zap} from 'lucide-react';
export default function Developers(){return <><header className="topbar"><a href="/" className="brand"><Zap fill="currentColor" className="accent"/>PEAK<span className="brand-product">DEVELOPERS</span></a><a href="/" className="wallet-button"><ArrowLeft size={16}/>Back to swap</a></header><main className="docs"><span className="eyebrow accent">PEAK SWAP API / V1</span><h1>One integration.<br/>Every Peak buy link.</h1><p className="doc-intro">A Solana swap module that trades directly against Pump.fun, PumpSwap, Raydium and Meteora pools. Your application supplies the pair and amount. Peak builds one transaction containing the swap and its 1% fee, the customer signs it with their wallet, and Peak validates the signed transaction and sends it to Solana.</p>
<h2 id="activation">Activation</h2><p>This deployment starts with trading disabled. Configure the following server settings before activation. Never provide a seed phrase or customer private key.</p><div className="setup-item"><span>01</span><div><strong>Solana RPC endpoint</strong><p>Set <code>SOLANA_RPC_URL</code> to an HTTPS mainnet RPC URL from a paid provider (for example Helius, Triton or QuickNode) as a server secret. It must allow <code>getProgramAccounts</code>, which Peak uses to find Raydium and Meteora pools. The public mainnet endpoint rate-limits and blocks this.</p></div></div><div className="setup-item"><span>02</span><div><strong>Peak fee wallet</strong><p>Set <code>PEAK_FEE_WALLET</code> to the treasury’s public SOL address. Fund it with at least 0.001 SOL first so small fee transfers never fall below rent exemption. Fees arrive as plain SOL transfers; no referral accounts or token accounts are needed.</p></div></div><div className="setup-item"><span>03</span><div><strong>Integration access & activation</strong><p>Set a random server secret of at least 32 bytes as <code>PEAK_API_KEY</code>. Then set <code>PEAK_TRADING_ENABLED=true</code> and redeploy. Every order is rejected if its transaction does not contain the exact fee transfer to the configured wallet.</p></div></div><p><strong>Access:</strong> This Site is initially private to its owner. An API key does not bypass Sites access controls. External buy links and server integrations require the Site to be made publicly accessible by the owner; the API continues to require its own bearer key for server mutations.</p>
<h2>API contract</h2><p><a href="/openapi.json">Download the OpenAPI specification</a></p><p>Base path: <code>/api/v1</code>. Server integrations send <code>Authorization: Bearer YOUR_PEAK_API_KEY</code>. Keep this key on your backend. Browser transactions use the same-origin Peak interface; cross-origin browser calls are rejected.</p><table><thead><tr><th>Method</th><th>Endpoint</th><th>Purpose</th></tr></thead><tbody><tr><td>GET</td><td><code>/health</code></td><td>Readiness, missing setup and fee policy.</td></tr><tr><td>GET</td><td><code>/tokens?query=…</code></td><td>Token metadata by mint address (SOL and USDC by symbol).</td></tr><tr><td>POST</td><td><code>/orders</code></td><td>Fee-checked quote and unsigned transaction.</td></tr><tr><td>POST</td><td><code>/execute</code></td><td>Validate the signed message and send it to Solana.</td></tr><tr><td>GET</td><td><code>/orders/:id</code></td><td>Read confirmation state.</td></tr></tbody></table>
<h2>Create → sign → execute</h2><pre><code>{`// POST /api/v1/orders — amount is an integer string in atomic units\n{\n  "inputMint": "So11111111111111111111111111111111111111112",\n  "outputMint": "TOKEN_MINT_ADDRESS",\n  "amount": "100000000",\n  "taker": "CUSTOMER_PUBLIC_WALLET_ADDRESS",\n  "slippageBps": 100,\n  "source": "peak-buybot"\n}\n\n// Returns { id, expiresAt, transaction, quote }\n// transaction is base64. Deserialize as a VersionedTransaction.\n// Have the customer's wallet sign without changing its message.\n\n// POST /api/v1/execute\n{ "id": "ORDER_ID", "signedTransaction": "BASE64_SIGNED_TRANSACTION" }\n\n// GET /api/v1/orders/ORDER_ID\n// Only status=confirmed means a confirmed purchase.\n// rejected / expired: never landed, no funds moved. unknown: poll again.`}</code></pre><p>Order IDs are unguessable access tokens: keep them private. On a timeout, poll the same order and retry only the exact same signed transaction. Do not create a replacement purchase automatically. No endpoint accepts a private key.</p>
<h2>Buy links</h2><pre><code>{`/?outputMint=TOKEN_MINT&ref=GROUP_OR_CAMPAIGN_ID`}</code></pre><p>The optional reference is stored with the order for attribution. It is not proof of a payable referral and does not create automatic partner payouts. The customer still chooses the amount and approves the transaction.</p>
<h2>Fee policy</h2><p>Peak charges <strong>100 basis points (1%)</strong> in SOL, added to the swap transaction as a transfer to Peak’s fee wallet. On buys it is 1% of the SOL amount and is charged on top: <code>amount</code> goes entirely into the pool and the customer spends <code>quote.totalInputAmount</code>. On sells it is 1% of the quoted SOL proceeds, taken after the swap, and <code>quote.outAmount</code> and <code>quote.minimumReceived</code> are already net of it. The pool’s own fees (Pump.fun protocol and creator fees, LP fees) are included in the quoted output. Network fees and rent for new token accounts are extra. There is no third-party share.</p>
<h2 id="coverage">Routing & limitations</h2><p>Supported venues: the <strong>Pump.fun bonding curve</strong> (SOL-quoted coins, before migration), <strong>PumpSwap</strong> canonical pools (after migration), <strong>Raydium CPMM</strong> and <strong>AMM v4</strong>, and <strong>Meteora DAMM v2</strong> and <strong>DLMM</strong>. For each quote Peak finds that token’s SOL pools on those venues, simulates the most liquid ones against current chain state, and uses the single pool that returns the most.</p><p>Every swap is a single hop with SOL on one side. Token-to-token swaps (for example USDC → a memecoin) and split routes are not supported. Pools on other venues (Raydium CLMM and LaunchLab, Meteora DAMM v1 and Dynamic Bonding Curve, Orca and others) are not routed. Migration can briefly interrupt liquidity. A rejected route needs a fresh quote, not blind resubmission.</p><p>Chart providers index the pool that was actually traded.</p><h2>Transaction controls</h2><ul><li>Customer wallet signing; no custody or private keys.</li><li>Server-fixed fee wallet and 1% fee; callers cannot override them. Orders are refused unless the exact fee transfer is in the transaction.</li><li>45-second maximum quote lifetime; each quote is simulated on-chain before it is returned.</li><li>Signed message matching and Ed25519 customer signature verification.</li><li>Atomic order claims, stored outcomes, and chain status checked before any retry is re-sent.</li><li>Slippage limited to 0.1–5%; price impact limited to 10%.</li><li>Priority fee cap of 0.001 SOL; quote shows network and account costs.</li><li>Per-IP rate limits, JSON size limits, same-origin protection and a trading kill switch.</li></ul><p>These controls reduce specific risks; they do not guarantee token safety or prevent every form of MEV. Mainnet execution on each venue and fee receipts still require small funded acceptance tests after activation.</p><h2>Integration notes</h2><p>Wallet support in this interface: injected Phantom and Solflare, including their in-app mobile browsers. No embedded custodial wallet. A future Peak Buy Bot can call the API from its backend and return the transaction to a customer-controlled signer. Confirmation polling is implemented; outbound webhooks are not enabled.</p></main></>}
