import {ArrowLeft,Zap} from 'lucide-react';
export default function Developers(){return <><header className="topbar"><a href="/" className="brand"><Zap fill="currentColor" className="accent"/>PEAK<span className="brand-product">DEVELOPERS</span></a><a href="/" className="wallet-button"><ArrowLeft size={16}/>Back to swap</a></header><main className="docs"><span className="eyebrow accent">PEAK SWAP API / V1</span><h1>One integration.<br/>Every Peak buy link.</h1><p className="doc-intro">A Solana swap module using Jupiter’s managed order and execution service. Your application supplies the pair and amount. The customer signs with their wallet. Peak validates the signed transaction and submits it to Jupiter.</p>
<h2 id="activation">Activation</h2><p>This deployment starts with trading disabled. Configure the following server settings before activation. Never provide a seed phrase or customer private key.</p><div className="setup-item"><span>01</span><div><strong>Jupiter API access</strong><p>Add <code>JUPITER_API_KEY</code> as a server secret from the <a href="https://developers.jup.ag/portal" target="_blank" rel="noreferrer">Jupiter portal</a>.</p></div></div><div className="setup-item"><span>02</span><div><strong>Peak fee collection</strong><p>Create a Jupiter referral account owned by Peak’s treasury using the <a href="https://referral.jup.ag" target="_blank" rel="noreferrer">referral dashboard</a>. Set its public address as <code>JUPITER_REFERRAL_ACCOUNT</code>. Initialize referral token accounts for SOL, USDC and each additional fee mint. This is a referral account address, not the treasury wallet address.</p></div></div><div className="setup-item"><span>03</span><div><strong>Integration access & activation</strong><p>Set a random server secret of at least 32 bytes as <code>PEAK_API_KEY</code>. After confirming fee accounts, set <code>PEAK_TRADING_ENABLED=true</code> and redeploy. Trading remains blocked whenever the quote does not apply the configured fee.</p></div></div><p><strong>Access:</strong> This Site is initially private to its owner. An API key does not bypass Sites access controls. External buy links and server integrations require the Site to be made publicly accessible by the owner; the API continues to require its own bearer key for server mutations.</p>
<h2>API contract</h2><p><a href="/openapi.json">Download the OpenAPI specification</a></p><p>Base path: <code>/api/v1</code>. Server integrations send <code>Authorization: Bearer YOUR_PEAK_API_KEY</code>. Keep this key on your backend. Browser transactions use the same-origin Peak interface; cross-origin browser calls are rejected.</p><table><thead><tr><th>Method</th><th>Endpoint</th><th>Purpose</th></tr></thead><tbody><tr><td>GET</td><td><code>/health</code></td><td>Readiness, missing setup and fee policy.</td></tr><tr><td>GET</td><td><code>/tokens?query=…</code></td><td>Token search and metadata from Jupiter.</td></tr><tr><td>POST</td><td><code>/orders</code></td><td>Fee-checked quote and unsigned transaction.</td></tr><tr><td>POST</td><td><code>/execute</code></td><td>Validate the signed message and submit it.</td></tr><tr><td>GET</td><td><code>/orders/:id</code></td><td>Read confirmation state.</td></tr></tbody></table>
<h2>Create → sign → execute</h2><pre><code>{`// POST /api/v1/orders — amount is an integer string in atomic units\n{\n  "inputMint": "So11111111111111111111111111111111111111112",\n  "outputMint": "EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v",\n  "amount": "100000000",\n  "taker": "CUSTOMER_PUBLIC_WALLET_ADDRESS",\n  "slippageBps": 100,\n  "source": "peak-buybot"\n}\n\n// Returns { id, expiresAt, transaction, quote }\n// transaction is base64. Deserialize as a VersionedTransaction.\n// Have the customer's wallet sign without changing its message.\n\n// POST /api/v1/execute\n{ "id": "ORDER_ID", "signedTransaction": "BASE64_SIGNED_TRANSACTION" }\n\n// GET /api/v1/orders/ORDER_ID\n// Only status=confirmed means a confirmed purchase.`}</code></pre><p>Order IDs are unguessable access tokens: keep them private. On a timeout, poll the same order and retry only the exact same signed transaction. Do not create a replacement purchase automatically. No endpoint accepts a private key.</p>
<h2>Buy links</h2><pre><code>{`/?outputMint=TOKEN_MINT&ref=GROUP_OR_CAMPAIGN_ID`}</code></pre><p>The optional reference is stored with the order for attribution. It is not proof of a payable referral and does not create automatic partner payouts. The customer still chooses the amount and approves the transaction.</p>
<h2>Fee policy</h2><p>The configured integrator fee is <strong>125 basis points (1.25%)</strong>: Peak’s share is 1.00% and Jupiter’s share is 0.25%. Pool costs, network costs and token account creation can be additional. Quotes with unexpected fees, including extra gasless recovery charges, are rejected rather than silently changing the price.</p>
<h2 id="coverage">Routing & limitations</h2><p>No venue restrictions are applied: all Jupiter managed routing engines remain eligible. Supported pre-migration curves and post-migration pools are discovered by Jupiter at quote time. Coverage is not a guarantee for every token, pool or launchpad. Migration can temporarily interrupt liquidity. A rejected route needs a fresh quote, not blind resubmission.</p><p>Chart providers index the pools actually used. RFQ trades may not appear as a swap on a particular AMM chart. Peak does not force routes to influence chart activity.</p>
<h2>Transaction controls</h2><ul><li>Customer wallet signing; no custody or private keys.</li><li>Server-fixed referral account and 1.25% fee; callers cannot override them.</li><li>45-second maximum quote lifetime, shortened by Jupiter’s expiry.</li><li>Signed message matching and Ed25519 customer signature verification.</li><li>Atomic order claims, stored outcomes and same-transaction retry checks.</li><li>Slippage limited to 0.1–5%; price impact limited to 10%.</li><li>Priority fee cap of 0.001 SOL; quote shows network and account costs.</li><li>Per-IP rate limits, JSON size limits, same-origin protection and a trading kill switch.</li></ul><p>These controls reduce specific risks; they do not guarantee token safety or prevent every form of MEV. Mainnet execution, fee receipts and venue-specific chart visibility still require funded acceptance testing after activation.</p><h2>Integration notes</h2><p>Wallet support in this interface: injected Phantom and Solflare, including their in-app mobile browsers. No embedded custodial wallet. A future Peak Buy Bot can call the API from its backend and return the transaction to a customer-controlled signer. Confirmation polling is implemented; outbound webhooks are not enabled.</p></main></>}
